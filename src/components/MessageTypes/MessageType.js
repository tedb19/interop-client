import React from 'react'

import { MainContent } from '../shared/Content/MainContent'

export const MessageType = (props) => {
    return (
        <MainContent
            sectionHeader="Message Type"
            />
    )
}